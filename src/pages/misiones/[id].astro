---
import Layout from '@/layouts/Layout.astro';
import MisionSelector from "@/components/missions/MissionSelector";

const { id } = Astro.params;
const { title } = Astro.props;
---

<Layout title={title || "Misión"}>
  <MisionSelector id={id} client:load />
</Layout>